% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bedload_functions.R
\name{smt}
\alias{smt}
\title{Estimating bedload transport using Smart (1984)}
\usage{
smt(
  R,
  S,
  d,
  d90_30 = 3.4,
  d84 = 2 * d,
  t_crit = 0.047,
  g = 9.81,
  rho = 1000,
  rho_s = 2650,
  a1 = 6.5,
  a2 = 2.5,
  U = u_ferg(R, S, d84, a1, a2, g)
)
}
\arguments{
\item{R}{hydraulic radius for the flow (m)}

\item{S}{gradient of the water surface slope (m/m)}

\item{d}{median bed sediment grain diameter (m)}

\item{d90_30}{ratio of the 90th and the 30th percentile of the gsd. Defaults to 3.4 (from a log normal distribution)}

\item{d84}{84th percentile diameter of bed sediment (m). Defaults to 2 * d}

\item{t_crit}{critical dimensionless shear stress. Defaults to 0.047}

\item{g}{the acceleration of gravity. defaults to 9.81 m/s2}

\item{rho}{fluid density. Defaults to 1000 kg/m3}

\item{rho_s}{sediment density. Defaults to 2650 kg/m3}

\item{a1}{constant fit by Ferguson (2007). Defaults to 6.5}

\item{a2}{constant fit by Ferguson (2007). Defaults to 2.5}

\item{U}{mean flow velocity. Defaults to flows calculated using Ferguson (2007)}
}
\description{
This function uses the Smart (1984) equation to estimate the dimensionless
bedload flux for steep channels (slopes 3-25 percent). The equation extends
Meyer-Peter & Müller to steep slopes by incorporating an explicit slope
term and a grain size non-uniformity factor. It is appropriate for coarse
sediment (d > 0.4 mm) in steep gravel- or cobble-bed channels.
}
